{% macro button(props, attr = '', tag = 'button') %}
<{{ tag }} class="button{% if props.class %} {{ props.class }}{% endif %}" {% if props.href %} href="{{ props.href }}"{% endif %} {{ props.type }} {{ attr }}>
	{{ props.text }}
</{{ tag }}>
{% endmacro %}
